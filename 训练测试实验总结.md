# 训练与测试实验总结

> 汇总范围：`projects/lunwen` 与 `projects/SCTransNet-main`  
> 生成时间：2026-06-23  
> 硬件：NVIDIA GeForce RTX 3090  
> Conda 环境：`lunwen`

---

## 1. 总体概览

| 项目 | 任务类型 | 已完成训练 | 已完成测试 | 进行中/待跑 |
|------|----------|------------|------------|-------------|
| `lunwen/SCTransNet` | DenseSIRST 损失函数 baseline 对比 | 3 组完整 + 2 组中断/未完成 | 2 组正式 test + 训练内嵌 test | 4 个 v2 baseline 待调度 |
| `SCTransNet-main` | 四数据集独立 SCTransNet 训练 | 4 数据集均 1000 epoch | 4 数据集均完成 test | 3 次四数据集批量启动未完成 |

**指标说明（与代码输出一致）：**

- `pixAcc` / `mIoU` / `nIoU` / `Pd` / `F1`：百分比（×100）
- `Fa`：False Alarm，单位为 ×10⁻⁶（与原始 SCTransNet README 一致）
- `lunwen` 的 CSV 记录中指标为 0~1 小数，本文表格中已换算为百分比

**DenseSIRST 数据划分（v2 split，审计通过）：**

| Split | 样本数 |
|-------|--------|
| train_v2 | 512 |
| val_v2 | 256 |
| test_v2 | 256 |
| 合计 | 1024（无 overlap） |

归一化：`mean=102.51`, `std=39.37`

---

## 2. 公共训练配置

### 2.1 `lunwen/SCTransNet`（改进版训练脚本）

| 参数 | 默认值 / 说明 |
|------|----------------|
| 模型 | SCTransNet，Deep Supervision=True |
| Optimizer | Adam |
| LR 调度 | Warmup + CosineAnnealing（`utils.get_optimizer`） |
| batchSize | 16（weighted_bce 早期实验为 8） |
| patchSize | 256 |
| threshold | 0.5 |
| seed | 42 |
| GPU | 默认 4 |
| 数据 split | train_v2 / val_v2 / test_v2 |

**v2 baseline 标准策略（`run_single_baseline.sh`）：**

| 参数 | 值 |
|------|-----|
| epochs | 1000 |
| begin_val | 500 |
| val_interval | 10 |
| val_max_samples | 0（全量 val） |
| test_interval | 50 |
| full_val_interval | 1 |

**选模规则（v1 / v2 共用，`train.py`）：** `*_best.pth.tar` **仅由 val mIoU 创新高时保存**；`test_interval>0` 时训练过程会周期性在 test 上评估并记录，**不参与选模**。事后 `run_test_record.sh` 应对 val-best checkpoint 做一次正式 test。

### 2.2 `SCTransNet-main`（原版训练脚本）

| 参数 | 值 |
|------|-----|
| 模型 | SCTransNet |
| Optimizer | Adam，lr=0.001 |
| Scheduler | CosineAnnealingLR，eta_min=1e-5 |
| epochs | 1000 |
| begin_test | 500 |
| every_test | 1（从 epoch 500 起每 epoch 验证） |
| batchSize | 16 |
| patchSize | 256 |
| threshold | 0.5 |
| seed | 42 |

---

## 3. `projects/lunwen` — DenseSIRST Baseline 实验

路径根目录：`projects/lunwen/SCTransNet`

### 3.1 已完成：baseline_original_bce（v2 策略）

**实验名：** `baseline_original_bce`  
**运行 ID：** `20260623_043618`  
**状态：** ✅ 训练完成（1000 epoch），训练内嵌 test 完成；`run_test_record.sh` 事后测试失败（路径校验问题）

| 参数 | 值 |
|------|-----|
| loss_mode | bce |
| lr | 0.001 |
| pos_weight | 50.0 |
| config_tag | v2_begin500_val10_test50 |
| GPU | 4 |
| 训练时长 | ~6h10m（04:36 → 10:46，2026-06-23） |

**训练结果：**

| 指标 | 值 |
|------|-----|
| 最终 epoch | 1000 |
| 最佳 val epoch | **730** |
| 最佳 val mIoU | **82.28%** |
| 最佳 val pixAcc | 89.23% |
| 最佳 val Pd | 97.42% |
| 最佳 val Fa | 2.38 ×10⁻⁶ |

**Checkpoint（official，val 选模）：**

- val-best：`log/baselines/DenseSIRST/baseline_original_bce/SCTransNet_730_best.pth.tar`（`EXPERIMENT_DONE best_epoch=730`）
- 正式 test：应对上述 ckpt 跑 `run_test_record.sh`（当前失败，尚无 official test 行写入 `test_results.csv`）

**训练中 test 观测（`test_interval=50`，非选模、非 official 结果）：**

| 来源 epoch | pixAcc | mIoU | Pd | Fa (×10⁻⁶) | 备注 |
|------------|--------|------|-----|------------|------|
| 850（test mIoU 最高的一次观测） | 88.93% | 83.21% | 98.73% | 0.81 | CSV `split=test`，**未**保存为 `*_best.pth.tar` |
| 1000（末次 test 观测） | 89.27% | 83.14% | 98.61% | 0.79 | 控制台 `TEST_RECORD` |
- 记录 CSV：`records/DenseSIRST/baseline_original_bce_20260623_043618.csv`

---

### 3.2 已完成：baseline_original_bce（v1 策略 + 归档 retest）

**实验名：** `baseline_original_bce`（v1）  
**运行 ID：** `20260622_131941`  
**状态：** ✅ 训练完成；checkpoint 已归档；✅ 独立 retest 完成

| 参数 | 值 |
|------|-----|
| loss_mode | bce |
| lr | 0.001 |
| begin_val / val_interval | 500 / **1**（每 epoch 全量 val） |
| test_interval | **0**（训练期间不跑 test） |
| config_tag | v1_begin500_val1_test0 |

**训练结果：**

| 指标 | 值 |
|------|-----|
| 最终 epoch | 1000 |
| 最佳 val epoch | **591** |
| 最佳 val mIoU | **82.61%** |
| 最佳 val pixAcc | 89.12% |

**归档后 test（2026-06-23 09:09，best ckpt epoch 591）：**

| pixAcc | mIoU | nIoU | Pd | Fa (×10⁻⁶) | F1 |
|--------|------|------|-----|------------|-----|
| 90.18% | 82.87% | 82.79% | 98.44% | 1.74 | 90.63% |

- Checkpoint（归档）：`log/baselines/DenseSIRST/baseline_original_bce_v1_val1_20260623_043613/SCTransNet_591_best.pth.tar`
- 测试记录：`log/baselines/test_records/test_results.csv`
- 记录 CSV：`records/DenseSIRST/baseline_original_bce_20260622_131941.csv`

---

### 3.3 已完成：baseline_weighted_bce_fast

**实验名：** `baseline_weighted_bce_fast`  
**运行 ID：** `20260622_000157`（GPU4 最终完整跑）  
**状态：** ✅ 训练完成（200 epoch），含训练内嵌 test

| 参数 | 值 |
|------|-----|
| loss_mode | weighted_bce |
| lr | 0.0001 |
| pos_weight | 80.0 |
| epochs | 200 |
| begin_val | 10 |
| val_interval | 10 |
| val_max_samples | 64（quick val） |
| full_val_interval | 50 |
| test_interval | 50 |
| early_stop_patience | 80 |
| min_epochs | 50 |

**训练结果：**

| 指标 | 值 |
|------|-----|
| 最终 epoch | 200 |
| 最佳 val epoch | **200** |
| 最佳 val mIoU | **29.71%** |
| 最佳 val pixAcc | 94.89% |

**测试集结果（epoch 200，TEST_RECORD）：**

| pixAcc | mIoU | Pd | Fa (×10⁻⁶) |
|--------|------|-----|------------|
| 96.28% | 29.12% | 18.30% | 359.4 |

> 注：mIoU 明显偏低，pred 前景比例偏大，weighted BCE 在该配置下分割质量不佳。

- Checkpoint：`log/baselines/DenseSIRST/baseline_weighted_bce_fast/SCTransNet_200_best.pth.tar`
- 记录 CSV：`records/DenseSIRST/baseline_weighted_bce_fast_20260622_000157.csv`

**同实验早期中断尝试（仅记录，未完成）：**

| 运行 ID | 进度 | 说明 |
|---------|------|------|
| 20260621_233203 | 43 epoch | 中断 |
| 20260621_235045 | 5 epoch | 中断 |

---

### 3.4 未完成 / 中断实验

#### baseline_original_bce 首次启动（20260622_131404）

- 进度：13 epoch 后中断
- 参数：与 v2 类似，lr=0.001, bce
- 记录：`records/DenseSIRST/baseline_original_bce_20260622_131404.csv`

#### baseline_weighted_bce

- 进度：**3 epoch** 后停止
- 参数：weighted_bce, lr=0.0001, pos_weight=80, batchSize=8, begin_val=1, val_interval=1, test_interval=5, threshold_sweep_interval=50
- 最佳 val（epoch 2）：mIoU **0.14%**，严重过预测 foreground
- 记录：`records/DenseSIRST/baseline_weighted_bce_20260621_215702.csv`
- 审计：`log/audit/DenseSIRST_20260621_215126/`（含 threshold sweep）

---

### 3.5 待运行：v2 Baseline 队列

调度器状态文件：`log/baselines/scheduler/state.tsv`（2026-06-23）

| 实验名 | 状态 | 参数 |
|--------|------|------|
| baseline_original_bce | ✅ 已完成（见 3.1） | bce, lr=0.001 |
| baseline_lr1e4 | ⏳ pending | bce, lr=0.0001 |
| baseline_bce_dice | ⏳ pending | bce_dice, lr=0.0001, bce=1, dice=1 |
| baseline_tversky | ⏳ pending | tversky, lr=0.0001, α=0.7, β=0.3 |
| baseline_focal_tversky | ⏳ pending | focal_tversky, lr=0.0001, γ=0.75 |

脚本入口：`scripts/run_baselines.sh` / `scripts/gpu4_baseline_scheduler.sh`

---

## 4. `projects/SCTransNet-main` — 四数据集 SCTransNet 训练

路径根目录：`projects/SCTransNet-main`  
成功方案：**双 GPU 双数据集并行**（2026-06-21 23:52 启动）

### 4.1 GPU4：DenseSIRST + IRSTD-1K

结果目录：`results/two_datasets_gpu4_20260621_235224/`  
日志：`nohup_gpu4_two_datasets.log`

| 数据集 | 训练 | 最佳 ckpt epoch | 测试 pixAcc | mIoU | nIoU | Pd | Fa (×10⁻⁶) | F1 |
|--------|------|---------------|-------------|------|------|-----|------------|-----|
| **DenseSIRST** | ✅ 1000 ep | 670 | 89.74% | **84.04%** | 84.22% | 98.93% | 0.57 | 91.33% |
| **IRSTD-1K** | ✅ 1000 ep | 568 | 78.63% | **67.28%** | 66.32% | 92.93% | 19.59 | 80.44% |

- DenseSIRST ckpt：`log/DenseSIRST/SCTransNet_670_best.pth.tar`
- IRSTD-1K ckpt：`log/IRSTD-1K/SCTransNet_568_best.pth.tar`
- 完成时间：2026-06-22 23:11

---

### 4.2 GPU1：NUDT-SIRST + SIRSTv1

结果目录：`results/two_datasets_gpu1_20260621_235224/`  
日志：`nohup_gpu1_two_datasets.log`

> 说明：00:01 曾切换为 GPU1 并行模式，首次 test 因 checkpoint 未就绪失败；09:18 重启 remainder 脚本后完成 test。

| 数据集 | 训练 | 最佳 ckpt epoch | 测试 pixAcc | mIoU | nIoU | Pd | Fa (×10⁻⁶) | F1 |
|--------|------|---------------|-------------|------|------|-----|------------|-----|
| **NUDT-SIRST** | ✅ 1000 ep | 575 | 96.28% | **93.97%** | 94.50% | 99.37% | 4.30 | 96.89% |
| **SIRSTv1** | ✅ 1000 ep | 750 | 88.63% | **77.87%** | 78.54% | 96.95% | 31.23 | 87.56% |

- NUDT-SIRST ckpt：`log/NUDT-SIRST/SCTransNet_575_best.pth.tar`
- SIRSTv1 ckpt：`log/SIRSTv1/SCTransNet_750_best.pth.tar`
- 完成时间：2026-06-22 09:21

---

### 4.3 未完成：四数据集批量启动

以下三次启动均只跑了第一个数据集或未产生完整 summary，**无有效 test 结果**：

| 结果目录 | 启动时间 | GPU | 状态 |
|----------|----------|-----|------|
| `results/four_datasets_20260621_234023` | 23:40:23 | 1 | 仅 TRAIN DenseSIRST 开始 |
| `results/four_datasets_parallel_20260621_234334` | 23:43:34 | 1+4 并行 | 仅 DenseSIRST + IRSTD-1K 启动 |
| `results/four_datasets_20260621_234759` | 23:47:59 | 4 | 仅 TRAIN DenseSIRST 开始 |

---

## 5. 跨项目 DenseSIRST 结果对比

同一模型 SCTransNet、同一数据集 DenseSIRST 的 test 对比：

| 来源 | 配置 | val-best ckpt | 指标类型 | mIoU | pixAcc | Pd | Fa (×10⁻⁶) |
|------|------|---------------|----------|------|--------|-----|------------|
| SCTransNet-main | 原版脚本，默认 bce | ep 670 (val) | official test | **84.04%** | 89.74% | 98.93% | **0.57** |
| lunwen v2 | bce, lr=0.001, v2 策略 | ep **730** (val) | val-best mIoU | 82.28% | 89.23% | 97.42% | 2.38 |
| lunwen v2 | 同上 | ep 730 (val) | official test | *待补跑* | *待补跑* | *待补跑* | *待补跑* |
| lunwen v2 | 同上 | — | 训练中 test 观测@850（非选模） | 83.21% | 88.93% | 98.73% | 0.81 |
| lunwen v1 retest | bce, lr=0.001, v1 策略 | ep 591 (val) | official test | 82.87% | **90.18%** | 98.44% | 1.74 |
| lunwen weighted_bce_fast | weighted_bce, 200 ep | ep 200 (val) | 训练中 test@200 | 29.12% | 96.28% | 18.30% | 359.4 |

> lunwen 与 SCTransNet-main 使用不同数据 split 版本（main 为各数据集原生 split，lunwen 为 v2 重划分），数值仅供趋势参考，不宜直接当作同条件对比。

---

## 6. 关键文件索引

### lunwen

| 类型 | 路径 |
|------|------|
| 训练脚本 | `SCTransNet/train.py` |
| Baseline 批量脚本 | `SCTransNet/scripts/run_baselines.sh` |
| 测试结果汇总 | `SCTransNet/log/baselines/test_records/test_results.csv` |
| 训练 CSV 记录 | `SCTransNet/records/DenseSIRST/*.csv` |
| 数据审计 | `SCTransNet/log/audit/DenseSIRST_20260621_215126/audit_summary.json` |
| 调度状态 | `SCTransNet/log/baselines/scheduler/state.tsv` |

### SCTransNet-main

| 类型 | 路径 |
|------|------|
| 训练脚本 | `train.py` |
| GPU4 双数据集脚本 | `run_two_datasets_gpu4.sh` |
| GPU1 双数据集脚本 | `run_two_datasets_gpu1.sh` |
| 完整 summary | `results/two_datasets_gpu4_20260621_235224/summary.txt` |
| 完整 summary | `results/two_datasets_gpu1_20260621_235224/summary.txt` |
| Checkpoint | `log/<DATASET>/SCTransNet_*_best.pth.tar` |

---

## 7. 已知问题与后续建议

1. **v2 baseline_original_bce 事后 test 失败**：`run_test_record.sh` 报 `checkpoint must be under .../log/`；**勿用**训练中 test@850 的 mIoU 83.21% 作为 official 结果（该 epoch 非 val-best）。应修复脚本路径校验后，对 **`SCTransNet_730_best.pth.tar`（val-best）** 补跑唯一一次正式 test。
2. **scheduler 状态未更新**：`state.tsv` 仍显示 `baseline_original_bce|running`，其余 4 个 baseline 为 pending，需手动启动 `run_baselines.sh` 或调度器继续队列。
3. **weighted_bce 系列**：当前 pos_weight=80 导致严重 foreground 过预测，后续实验前建议重新标定 pos_weight 或换 loss。
4. **SCTransNet-main 四数据集串行脚本**：三次启动均未跑完，已成功方案为 `run_two_datasets_gpu{1,4}.sh` 双卡并行。

---

*本文档由实验日志、CSV 记录与 summary 文件自动汇总生成。*
